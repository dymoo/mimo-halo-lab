{
  "schema_version": 1,
  "recorded_at_utc": "2026-09-22T19:05:00Z",
  "purpose": "Arrival readiness for the Strix Halo: every asset/config/command on the first-boot path with the proof that exists for it, the missing prerequisite, and the next command. Nothing here claims Halo execution.",
  "evidence_classes": {
    "validated-on-Mac": "executed on the Mac workstation; exit code and output observed",
    "static-checked-for-Linux": "argv/flags/help/plan inspected against the pinned binaries and scripts; not executed on Linux",
    "untested-on-Halo": "requires the Strix Halo host over SSH or real gfx1151 hardware; no proof yet"
  },
  "machine_profiles": {
    "mac-prep": {
      "ram_bytes": 51539607552,
      "ram_gib": 48,
      "scope": "tiny runtime probes, layer-streamed/bounded scratch only; NO full ~90 GiB candidate load or full-model materialization on this machine; memory-heavy executions coordinate with MiMoObservationRun"
    },
    "strix-halo": {
      "ram_gib": 128,
      "status": "arriving; every Halo row below is untested-on-Halo until SSH is available"
    }
  },
  "identity_anchors": {
    "upstream_pins": {
      "evidence": "validated-on-Mac",
      "proof": "git rev-parse HEAD matches manifests/upstreams.json for all four: reap 1970473c51ca3caeb98c10392f15b3a08a672974, strix-llama.cpp 8c1c282ecb194e8f02613defcc4a07c22b6d1c08, rocmfp4 ddd08770a7f063380c35353bdd7c44d7604106a6, llama.cpp 38a5b42d9a3e82e0a586bcd1caed121f36c87a73"
    },
    "local_patches": {
      "evidence": "validated-on-Mac",
      "proof": "manifest patch shas hash-match patch files after b710049: strix-slot-restore-checkpoints.patch 2c28dccd0041342fbec8a9008b4f7d9567c617bb70cb71d4eef64168c9a24d65, strix-macos-prefetch.patch c199b3b31c1b2e5c0037824ff119fae2748d95f3455a454d50dcd7a5777abe7e. Patch-file vs applied-diff hashes differ only by the patch explanatory header (applied-diff body verified == tree by NvmeRestoreFix); compare patch FILE hashes, never diffs. Apply/confirm loop proven twice on a clean pinned worktree: apply fresh, reverse --check OK for both.",
      "missing": "a fresh Halo checkout from the manifest fetch_method is UNPATCHED",
      "next": "run the patch loop in docs/hardware-bringup.md step 12 on the Halo before building"
    },
    "smoke_fixture": {
      "evidence": "validated-on-Mac",
      "proof": "scratch/loadability/tiny_b_dense.gguf, 85916736 B, sha256 04399ecf52904ce316e06ea4b73d3820793ee07c0c9eff2d43efd735386d9647; synthetic graph smoke fixture generated by CandidateLoadability's probe -- NOT a known-good quality model and never a capability claim; executed on the pinned CPU build by Main: build-metal/bin/llama-cli RC0, ftype F16, 8 tokens, commit 8c1c282 (tiny synthetic CPU graph compatibility proof only, no model-quality or Halo speed claim). STAGED to $MIMO_LAB/source-models/tiny.gguf with copy + hash identity verified equal (STAGE-OK, 2026-09-22)",
      "missing": "Halo-side visibility of the same lab workspace (carried disk or rsync per step 23)",
      "next": "verify identity on the Halo with sha256sum against this hash before first use; re-stage only with cp -n plus hash identity -- never overwrite an existing target unverified"
    }
  },
  "assets": [
    {
      "id": "lab-workspace",
      "ref": "$MIMO_LAB (operator-set lab workspace root on the external volume; path never recorded here)",
      "evidence": "validated-on-Mac",
      "proof": "workspace.py status exit 0: 15/15 dirs, filesystem_free_bytes 3693049282560, model_staging allowed; manifests/workspace.json init record with verified hash smoke",
      "missing": "Halo-side workspace not created (Halo not reachable)",
      "next": "on Halo: export MIMO_LAB=... && python3 scripts/workspace.py init --min-model-free-bytes 128849018880 && python3 scripts/workspace.py status"
    },
    {
      "id": "mimo-source-weights",
      "ref": "$MIMO_LAB/source-models/XiaomiMiMo--MiMo-V2.6-Flash-RL/5711b268169967567844e1e560e8a3966da959b1",
      "evidence": "validated-on-Mac",
      "proof": "model.safetensors.index.json: 73081 tensors, 65/65 referenced safetensors present, 0 missing; config n_routed_experts=256, num_hidden_layers=48, fp8 e4m3; ~166 GiB on disk; original Xiaomi weights only",
      "missing": "Halo-side copy if the disk is not carried over",
      "next": "rsync -a --checksum per runbook step 23, then verify against manifests/models/mimo-v2.6-flash-rl/inventory.json lfs_sha256 digests"
    },
    {
      "id": "mimo2-trunk-gguf",
      "ref": "$MIMO_LAB/source-models/mimo2-trunk.gguf",
      "evidence": "validated-on-Mac (converter wiring only)",
      "proof": "--vocab-only export exit 0 via $REPO/.venv/bin/python (5948629 B, sha256 4de3bb14b22491d8a0a2dd5f1fa5f47285b97d3f126fba38b25a1eccb5a39563); full --dry-run exit 1: conversion/mimo.py:211 KeyError model.layers.1.mlp.experts.12.gate_proj.weight (expert-merge trigger n_routed_experts*3 fires early because .weight_scale keys also match 'mlp.experts' across 64 shard files in lexicographic ep0,ep1,ep10,... order)",
      "missing": "converter expert-merge fix (owned by NativeGgufBridge); system python3 unusable for transformers on this Mac (packaging metadata resolves None) -- must use $REPO/.venv/bin/python",
      "next": "after the fix: (cd $STRIX_SRC && $REPO/.venv/bin/python convert_hf_to_gguf.py $MIMO_LAB/source-models/XiaomiMiMo--MiMo-V2.6-Flash-RL/5711b268169967567844e1e560e8a3966da959b1 --outfile $MIMO_LAB/source-models/mimo2-trunk.gguf) -- and the output is the layer-streamed ORIGINAL quality reference ONLY: NEVER resident-load it (-ngl 999), source scale 172923364096 B observed (~161 GiB, larger after conversion) exceeds both 48 GiB Mac and 128 GB Halo"
    },
    {
      "id": "resident-candidate-gguf",
      "ref": "$CANDIDATE_DIR/$MODEL (selected pruned ~90 GiB candidate for resident serving/benchmarks)",
      "evidence": "untested-on-Halo",
      "proof": "no candidate GGUF built yet (pruned-models/ and quantized-models/ observed empty); resident -ngl 999 load of the unpruned trunk is NOT a valid arrival baseline (source scale exceeds both machines' RAM)",
      "missing": "candidate build (quality-first sweep, original Xiaomi weights only), then artifact-size preflight against the accepted band",
      "next": "runbook step 34 preflight: stat -c%s <= budget.window_bytes[1] 98569499443 (90 GiB +2% accepted upper band; target_resident_bytes 96636764160 alone would wrongly reject in-band REAP45 +0.84% / REAP25 +0.98%) AND MemAvailable >= bytes + 16 GiB FIRST_BOOT_RESERVE_ASSUMPTION; file size is a conservative artifact-size proxy only and does NOT equal resident weight memory -- final admission/quality reports use measured loader/runtime resident memory; then resident commands; BLOCKED until a real candidate exists -- no fake substitution"
    },
    {
      "id": "pinned-sources",
      "ref": "upstreams/{reap,strix-llama.cpp,rocmfp4,llama.cpp}",
      "evidence": "validated-on-Mac",
      "proof": "4/4 rev-parse HEAD == manifests/upstreams.json",
      "missing": "Halo-side checkouts (manifest fetch_method: shallow git init + checkout --detach, no weights, no build)",
      "next": "runbook steps 8-12 (rev-parse x4, manifest cross-check, patch loop)"
    },
    {
      "id": "metal-correctness-build",
      "ref": "upstreams/strix-llama.cpp/build-metal",
      "evidence": "validated-on-Mac",
      "proof": "llama-cli --version: 0.4.1-dev commit 8c1c282; CMakeCache Release/METAL=ON/VULKAN=OFF/HIP=OFF/TESTS=ON; ctest LastTest.log 164/164 passed, 0 Failed/Timeout markers",
      "missing": "none for Mac correctness scope",
      "next": "Halo backends are separate builds (runbook E)"
    },
    {
      "id": "halo-backend-builds",
      "ref": "build-vulkan / build-hip / build-cpu on the Halo",
      "evidence": "untested-on-Halo",
      "proof": "none -- no HIP/Vulkan execution has occurred or is claimed anywhere in this record; Mac has only build-metal (observed: no build-cpu/build-vulkan/build-hip dirs)",
      "missing": "SSH to $HALO; Vulkan ICD (RADV) and/or ROCm hipconfig for HIP",
      "next": "python3 scripts/build_runtime.py --source $STRIX_SRC --backend vulkan --run --yes  (hip additionally resolves HIPCXX/HIP_PATH from hipconfig at --run)"
    },
    {
      "id": "dataset-payloads",
      "ref": "$MIMO_LAB/datasets/ (real corpus per configs/dataset.json and docs/datasets.md partition/eligibility rules)",
      "evidence": "validated-on-Mac (observation only)",
      "proof": "$MIMO_LAB/datasets/ observed empty on 2026-09-22; no placeholder data is written anywhere",
      "missing": "the actual corpus payloads defined by configs/dataset.json, and their sidecar checksums.txt",
      "next": "materialize payloads per configs/dataset.json -> cd $MIMO_LAB/datasets && sha256sum <files> > checksums.txt (Linux) / shasum -a 256 <files> > checksums.txt (Mac) -> rsync -a --checksum per step 23 -> sha256sum -c checksums.txt on the receiving side"
    },
    {
      "id": "perplexity-slice",
      "ref": "$MIMO_LAB/datasets/perplexity-slice.txt (consumer: llama-perplexity --file in runbook step 34)",
      "evidence": "untested-on-Halo",
      "proof": "not materialized; intended source is the real corpus governed by configs/dataset.json / docs/datasets.md -- no synthetic stand-in may be used for recorded numbers",
      "missing": "the slice itself, cut from the intended corpus once materialized",
      "next": "cut the slice from the configs/dataset.json corpus with its provenance noted in the run record; until then the perplexity leg of step 34 stays blocked"
    },
    {
      "id": "hw-commands-list",
      "ref": "$REPO/configs/hw-commands.txt",
      "evidence": "static-checked-for-Linux",
      "proof": "file does not exist yet (operator-create by design); hardware_capture.py argument contract validated-on-Mac: missing --commands exit 2, missing destination exit 2, sudo never auto-injected",
      "missing": "the file, and Halo execution of the capture itself",
      "next": "runbook step 18 heredoc, then python3 scripts/hardware_capture.py --commands configs/hw-commands.txt"
    }
  ],
  "commands": [
    {
      "id": "workspace-tool",
      "ref": "scripts/workspace.py {init,status,smoke}",
      "evidence": "validated-on-Mac",
      "proof": "disposable-dir runs: init exit 0 (15 dirs, hash smoke verified), smoke exit 0 verified, status-on-absent exit 2 (root NOT created), missing MIMO_LAB exit 2, low-space init exit 1 with MODEL STAGING BLOCKED (4 model dirs withheld, 11 metadata dirs intact); real-lab status exit 0; no code fix needed",
      "missing": "Halo execution",
      "next": "identical commands on the Halo (asset row lab-workspace)"
    },
    {
      "id": "build-driver",
      "ref": "scripts/build_runtime.py",
      "evidence": "validated-on-Mac (plans + guard rails); builds untested-on-Halo",
      "proof": "plan exit 0 for cpu/metal/vulkan/hip with pin verified (hip plan carries -DGPU_TARGETS=gfx1151 and records HIPCXX/HIP_PATH requirements without substitution; runtime_env HIP_LAUNCH_BLOCKING=1, HSA_OVERRIDE_GFX_VERSION=11.5.1, GGML_HIP_ENABLE_UNIFIED_MEMORY=1); refusals all exit 2: --run without --yes, --run+--skip-pin-check, --jobs 0, unknown backend; no code fix needed",
      "missing": "SSH + backend toolchains",
      "next": "runbook E steps 25-29 via the driver"
    },
    {
      "id": "hardware-capture",
      "ref": "scripts/hardware_capture.py",
      "evidence": "static-checked-for-Linux (arg/guard paths validated-on-Mac); capture itself untested-on-Halo",
      "proof": "argument validation exit 2s observed; NO target-hardware command was run on the Mac (lspci/dmidecode/lscpu etc. are Halo-only by design)",
      "missing": "configs/hw-commands.txt and the Halo host",
      "next": "runbook D steps 18-20"
    },
    {
      "id": "smoke-launch",
      "ref": "pinned llama-cli tiny-fixture launch (runbook step 30-32 flag set)",
      "evidence": "validated-on-Mac for CPU/flags (Main RC0); Vulkan/HIP variants untested-on-Halo",
      "proof": "pinned --help confirms --single-turn, --prompt, --n-predict, --gpu-layers/--n-gpu-layers, --offline, --ctx-size, --threads, --seed, --temp, --simple-io; --no-conversation is REJECTED by this pin and must never appear; benchmark_matrix/llama-bench/llama-perplexity/llama-server flag surfaces also checked against pinned --help (-o json, -p, -n, -fa, -f/--file, --port all present), matching scripts/benchmark_matrix.py argv exactly",
      "missing": "Halo builds for the -ngl 999 variants",
      "next": "steps 30-32 after builds"
    },
    {
      "id": "benchmark-and-replay-args",
      "ref": "scripts/benchmark_matrix.py, scripts/burst_replay.py, configs/hardware-benchmarks.json",
      "evidence": "validated-on-Mac (argument validation + config load); measurement runs untested-on-Halo",
      "proof": "missing-required-arg exits 2 for both scripts; config loads: schema 1, PP [8192,32768,65536,131072], C [1,2,4,8], timeouts, runtime_controls hip/vulkan present; config read-only honored (not modified)",
      "missing": "Halo binaries and model",
      "next": "runbook G steps 38-46"
    }
  ],
  "external_blockers": [
    "SSH target $HALO unreachable until the Strix Halo arrives (deadline 2026-09-23 10:00 BST) -- gates every untested-on-Halo row",
    "gfx1151 hardware truth: RADV/ROCm driver presence, BIOS Gate D (UMA/GTT/IOMMU/SecureBoot), sudo-marked capture lines -- operator/hardware only",
    "converter expert-merge KeyError fix (NativeGgufBridge) before mimo2-trunk.gguf can exist",
    "real dataset corpus per configs/dataset.json not yet materialized; checksums.txt and perplexity-slice.txt derive from it -- no placeholder substitutes",
    "Mac scope hard limit: 48 GiB RAM -- no full candidate/conversion materialization on the Mac; full conversions belong on the Halo or a bounded streamed flow coordinated with MiMoObservationRun"
  ],
  "first_halo_sequence": [
    "export REPO=/path/to/repo",
    "export MIMO_LAB=/path/to/lab",
    "export HALO=user@halo",
    "export STRIX_SRC=\"$REPO/upstreams/strix-llama.cpp\"   # separate assignment so it never reads a prior STRIX_SRC value",
    "for r in reap strix-llama.cpp rocmfp4 llama.cpp; do git -C \"$REPO/upstreams/$r\" rev-parse HEAD; done   # must equal manifests/upstreams.json (steps 8-11)",
    "runbook step 12 patch loop over \"$REPO\"/patches/strix-*.patch (apply fresh; reverse --check both; sha256 == manifest)",
    "runbook step 18 printf one-liner -> \"$REPO/configs/hw-commands.txt\" (no heredoc; copy-paste safe)",
    "python3 \"$REPO/scripts/hardware_capture.py\" --commands \"$REPO/configs/hw-commands.txt\"   # raw stays under $MIMO_LAB/traces/private/",
    "python3 \"$REPO/scripts/workspace.py\" init --min-model-free-bytes 128849018880 && python3 \"$REPO/scripts/workspace.py\" status",
    "python3 \"$REPO/scripts/build_runtime.py\" --source \"$STRIX_SRC\" --backend vulkan --run --yes   # then cpu; hip when hipconfig exists",
    "ctest --test-dir \"$STRIX_SRC/build-vulkan\" --output-on-failure",
    "stage fixture: cp -n \"$REPO/scratch/loadability/tiny_b_dense.gguf\" \"$MIMO_LAB/source-models/tiny.gguf\"; sha256sum identity check == 04399ecf52904ce316e06ea4b73d3820793ee07c0c9eff2d43efd735386d9647 (synthetic fixture, not a quality model)",
    "\"$STRIX_SRC/build-cpu/bin/llama-cli\" --model \"$MIMO_LAB/source-models/tiny.gguf\" --offline --ctx-size 512 --n-gpu-layers 0 --threads 1 --seed 42 --temp 0 --n-predict 8 --single-turn --simple-io --prompt test   # then build-vulkan --n-gpu-layers 999, then HIP with step-28 control env",
    "[BLOCKED: converter fix] convert original weights -> $MIMO_LAB/source-models/mimo2-trunk.gguf = layer-streamed ORIGINAL quality reference ONLY, never resident-loaded (~161 GiB observed source scale > 128 GB Halo and > 48 GiB Mac)",
    "[BLOCKED: candidate not built] resident baseline needs $CANDIDATE_DIR/$MODEL (~$90 GiB): runbook step 34 artifact-size preflight (stat -c%s <= 98569499443 = budget.window_bytes[1], 90 GiB +2% accepted band; MemAvailable >= bytes + 16 GiB FIRST_BOOT_RESERVE_ASSUMPTION; file size is a proxy, NOT resident weight memory -- final reports measure loader/runtime resident memory), then llama-cli sanity; perplexity needs the slice from the configs/dataset.json corpus",
    "rsync -a --checksum datasets payloads (step 23) -> sha256sum -c checksums.txt -> gates E/F -> benchmark matrix steps 38+"
  ]
}
