[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mimo-halo"
version = "0.1.0"
description = "Reproducible MiMo compression, evaluation, and inference-state laboratory tooling"
requires-python = ">=3.11"
dependencies = []


[tool.setuptools.packages.find]
where = ["src"]